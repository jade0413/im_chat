# 复制为 .env 后修改。生产环境一律换强密码，且 .env 不入库（.gitignore）
MYSQL_ROOT_PASSWORD=imchatjade
MYSQL_PORT=3306

REDIS_PASSWORD=imchatjade
REDIS_PORT=6379

RABBITMQ_USER=admin
RABBITMQ_PASSWORD=admin123sfg
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672

MINIO_ROOT_USER=im_minio
MINIO_ROOT_PASSWORD=im_dev_minio_pwdffff
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
# 返回给浏览器/APP 的预签名 URL 地址；生产改成 https://你的文件域名 或 http://服务器IP:9000
MINIO_PUBLIC_ENDPOINT=http://localhost:9000

# im-server
JWT_SECRET=7GstW+YVw1MVFqVM1eSkf3aejUsT99TzW8DSJFRE2EiP6eQpb6U70FiVhPFOVdZN


# im-gateway
GATEWAY_PORT=8082
IM_GATEWAY_ALLOWED_ORIGINS=*
IM_GATEWAY_HANDSHAKE_RATE_LIMIT_PER_SEC=200
IM_GATEWAY_HANDSHAKE_RATE_LIMIT_BURST=400

GRAFANA_PASSWORD=admin

# ── 语音通话 TURN（D45）──────────────────────────────
# 服务器公网 IP：云服务器（NAT 公网）必填，coturn 用它对外宣告中继地址；
# 网卡直持公网 IP 或纯内网联调可留空（留空时 --external-ip 参数自动省略）
EXTERNAL_IP=103.45.65.84
# coturn static-auth-secret，与 im-server 签发 TURN 凭证共享，务必换随机长串
TURN_SECRET=jijlwieichoa@sjhjshj
TURN_REALM=im.local
