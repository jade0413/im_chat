spring:
  config:
    activate:
      on-profile: test
  rabbitmq:
    host: localhost
    port: 5672
    username: im
    password: im_dev_mq_pwd
    listener:
      simple:
        auto-startup: false
  flyway:
    enabled: false

management:
  health:
    db:
      enabled: false
    redis:
      enabled: false
    rabbit:
      enabled: false

im:
  grpc:
    port: 0
  id:
    lease:
      enabled: false
  message:
    retention:
      enabled: false
  outbox:
    enabled: false
  auth:
    jwt:
      secret: test-only-secret-test-only-secret-123456
      issuer: im-server
      access-ttl: 2h
      refresh-ttl: 30d
