spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/im?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:im_dev_mysql_pwd}
    driver-class-name: com.mysql.cj.jdbc.Driver

im:
  rpc:
    gateway:
      port: 9091
