spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://mysql:3306/im?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:im}
    password: ${RABBITMQ_PASSWORD}

im:
  auth:
    jwt:
      # 生产必须从环境注入；无默认值 → 未配置则启动即失败（fail-fast），
      # 避免误用 application.yml 里的弱开发常量密钥导致 JWT 可被伪造。
      secret: ${IM_AUTH_JWT_SECRET:${JWT_SECRET}}
  startup-check:
    enabled: ${IM_STARTUP_CHECK_ENABLED:true}
    minio-config:
      endpoint: ${MINIO_ENDPOINT:http://minio:9000}
      access-key: ${MINIO_ROOT_USER}
      secret-key: ${MINIO_ROOT_PASSWORD}
      bucket: ${MINIO_BUCKET:im-media}
  outbox:
    enabled: ${OUTBOX_ENABLED:true}
  rpc:
    gateway:
      port: 9091
